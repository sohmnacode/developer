const escape = value => String(value ?? '').replace(/[&<>"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const reportLink = (researcher, item = '') => `mailto:hello@reincarnatedai.com?subject=${encodeURIComponent(`Researcher profile correction: ${researcher.name}`)}&body=${encodeURIComponent(`Profile: https://reincarnatedai.com/researcher/${researcher.id}\nItem: ${item}\nWhat needs correcting: `)}`;
function renderResearcherProfile(r, photo, books = [], bookLinks = {}) {
  const url = `https://reincarnatedai.com/researcher/${r.id}`;
  const title = `${r.name} — Researcher Profile | ReincarnatedAI`;
  const description = String(r.headline || r.bio || '').slice(0, 180);
  const portrait = photo
    ? `<img class="portrait" src="${escape(photo.src)}" alt="Portrait of ${escape(r.name)}" width="320" height="320"><p class="photo-credit">Photo: ${escape(photo.credit)} · ${escape(photo.license)} · <a href="${escape(photo.source)}" target="_blank" rel="noopener">Image source and license ↗</a></p>`
    : `<div class="portrait placeholder" role="img" aria-label="No verified portrait available">${escape(r.name.split(' ').map(part => part[0]).slice(0,2).join(''))}</div><p class="photo-credit">A verified public portrait is not available yet.</p>`;
  const publications = (r.recentPapers || []).length
    ? r.recentPapers.map(p => `<article class="source-card"><div class="source-meta">${escape(p.year)} · ${escape(p.journal)}</div><h3>${escape(p.title)}</h3><div class="source-actions">${p.url ? `<a href="${escape(p.url)}" target="_blank" rel="noopener">Open source ↗</a>` : '<span>Source link pending verification</span>'}<a href="${escape(reportLink(r, p.title))}">Report a problem</a></div></article>`).join('')
    : '<p>No publication links have been verified for this profile yet. The linked researcher or institution page is a starting point for their work.</p>';
  const links = [r.institutionUrl, ...Object.values(r.links || {})].filter(Boolean);
  const uniqueLinks = [...new Set(links)];
  const authoredBooks = books.length ? `<section><h2>Books by this researcher</h2><div class="sources">${books.map(book => `<article class="source-card"><div class="source-meta">Book · ${escape(book.year)} · ${escape(book.domain)}</div><h3>${escape(book.title)}</h3><p>${escape(book.note)}</p><div class="source-actions"><a href="${escape(bookLinks[book.title].url)}" target="_blank" rel="noopener">View book · ${escape(bookLinks[book.title].source)} ↗</a></div></article>`).join('')}</div><a class="profile-page-link" href="/book-publications">Browse all book publications →</a></section>` : '';
  return `<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="color-scheme" content="light"><title>${escape(title)}</title><meta name="description" content="${escape(description)}"><link rel="canonical" href="${url}"><meta property="og:type" content="article"><meta property="og:title" content="${escape(title)}"><meta property="og:description" content="${escape(description)}"><meta property="og:url" content="${url}"><meta property="og:image" content="https://reincarnatedai.com${photo ? escape(photo.src) : '/og-image.jpg'}"><link rel="icon" href="/favicon.ico"><link rel="stylesheet" href="/assets/theme.css"><link rel="stylesheet" href="/assets/research.css"><link rel="stylesheet" href="/assets/researcher-profile.css"></head><body><nav><a href="/" class="logo">ReincarnatedAI</a><div class="links"><a href="/evidence">Evidence Map</a><a href="/cases">Cases</a><a href="/researchers">Researchers</a><a href="/book-publications">Books</a><a href="/methodology">Methodology</a></div></nav><main class="profile-main"><a class="back" href="/researchers">← All researchers</a><header class="profile-header"><div class="portrait-wrap">${portrait}</div><div><p class="eyebrow">Researcher profile</p><h1>${escape(r.name)}</h1><p class="role">${escape(r.role)} · ${escape(r.institution)}</p><p class="headline">${escape(r.headline)}</p><div class="tags">${(r.domains || []).map(d=>`<span>${escape(d)}</span>`).join('')}</div></div></header><section class="glance"><h2>At a glance</h2><div class="glance-grid"><div><h3>Area of work</h3><p>${escape(r.headline)}</p></div><div><h3>Methods</h3><p>${escape(r.methodology)}</p></div><div><h3>Limits and debate</h3><p>${escape(r.controversy)}</p></div></div><p class="glance-note">This summarizes the researcher's work. It is not a finding that any theory of consciousness or survival has been proven.</p></section><section><h2>Overview</h2><p>${escape(r.bio)}</p></section>${authoredBooks}<section><h2>Key publications</h2><p class="section-intro">Publication details and source links are checked before being listed. Some older works have no verified online destination.</p><div class="sources">${publications}</div></section><section><h2>Researcher and institution links</h2><div class="external-links">${uniqueLinks.map((link,i)=>`<a href="${escape(link)}" target="_blank" rel="noopener">${i===0 ? 'Researcher or institution' : 'Additional source'} ↗</a>`).join('') || '<span>No verified website link yet.</span>'}</div></section><section class="correction"><h2>Help us keep this accurate</h2><p>If a source has moved, a portrait is outdated, or profile information needs correction, please tell us which item to review.</p><a href="${escape(reportLink(r))}">Report a correction ↗</a></section></main><footer class="footer"><span>Profiles synthesize published work; interpretations and evidence strength remain contested.</span><span><a href="/methodology">Methodology</a> · <a href="/researchers">Researchers</a></span></footer><script src="/assets/ui.js" defer></script></body></html>`;
}
module.exports = { renderResearcherProfile };
